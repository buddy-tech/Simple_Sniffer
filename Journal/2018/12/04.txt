Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.4
Creation-Date: 2018-12-04T09:14:36+08:00

====== Tuesday 04 Dec 2018 ======

* 数据帧的转发是依据目的 MAC 地址查询 CAM 表，而 CAM 表的学习则是以源 MAC 地址为依据

''在Python中并没有__builtins__这个模块，只有__builtin__模块，__builtins__模块只是在启动Python解释器时，解释器为我们自动创建的一个到__builtin__模块的引用。（在Python3中__builtin__模块命名为builtins）''
	* ''如果是在主模块__main__中，__builtins__直接引用__builtin__模块，此时模块名__builtins__与模块名__builtin__指向的都是同一个模块，即<builtin>内建模块（这里要注意变量名和对象本身的区别）''
	* ''如果不是在主模块中，那么__builtins__只是引用了__builtin__.__dict__''

* LRU（Least Recently Used，最近最少使用）是一种内存页面置换算法。它的思想是基于这样一种观察和假定：最近经常访问的数据，在所有数据中也是最常访问的。所以，在这样一块空间中，最近被访问过的页面被当做“频繁访问”的，而一直没有被访问过的则被替换出去。
	* Python模块：pylru

* 用交互式解释器执行python的话不会将当前路径记录到''__file__''中
* 当一个函数中包含yield语句时，python会自动将其识别为一个生成器
